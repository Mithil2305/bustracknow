/**
 * RoutePolyline – No-op on native.
 * Route rendering is now handled inside the Leaflet WebView (LiveMap.jsx).
 * This stub exists so imports from other files don't break.
 */
export default function RoutePolyline() {
  return null;
}
