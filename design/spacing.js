/**
 * Extended spacing scale for layout consistency.
 * Complements the base spacing in tokens.js.
 */
export const spacingScale = {
  /** 2px — hairline gaps */
  xxs: 2,
  /** 4px — tight spacing */
  xs: 4,
  /** 8px — compact */
  sm: 8,
  /** 12px — standard small */
  md: 12,
  /** 16px — default */
  lg: 16,
  /** 24px — comfortable */
  xl: 24,
  /** 32px — section gap */
  xxl: 32,
  /** 48px — large section */
  xxxl: 48,
  /** 64px — hero/header */
  huge: 64,
};

/**
 * Common layout helpers.
 */
export const layout = {
  screenPadding: 16,
  cardPadding: 12,
  sectionGap: 24,
  tabBarHeight: 60,
  headerHeight: 56,
};
